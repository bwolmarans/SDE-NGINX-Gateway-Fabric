apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: post-n-get-gateway
spec:
  gatewayClassName: nginx
  listeners:
  - name: http
    port: 80
    protocol: HTTP
    hostname: "*.lab.f5npi.net"